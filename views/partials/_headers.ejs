<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <% if(username.length){ %>
      <h6>You're logged in as:</h6>
      <%= username %>
      <form method="POST" action="/logout"><input type="submit"  value="Logout"/></form>
      <% } else{ %>  
        <form method="POST" action="/login"><input type="text" name ="username" placeholder="name"><input type="submit"  value="Login" /></form>      
        <% } %>

  <!-- use if statement to check is username is present, if yes display logout -->
  </nav>
</header>